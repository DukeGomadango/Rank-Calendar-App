# Commit message (avoid encoding issues on Windows)

Use **English** for commit messages to prevent garbled characters when pushing from Windows/PowerShell.

Example:
```
fix: resolve TypeScript build errors for Vercel
```
